# Django Core
SECRET_KEY=very secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Timezone
TIME_ZONE=UTC

# PostgreSQL
DB_NAME=algolab
DB_USER=algolab_user
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432

# Redis (for Celery)
REDIS_URL=redis://localhost:6379
